_build/
.rebar3/
_checkouts/
rebar3.crashdump

# Dependency versions are pinned in rebar.config, which is the provenance record.
# A lockfile would be a second, silently disagreeing source of truth.
rebar.lock

*.beam
*.plt
.eunit
erl_crash.dump
doc/

# Transient runner feeds at repo root (the signed copy lives in programmes/pN_*/)
/exp*_feed.txt
/exp*.champions
